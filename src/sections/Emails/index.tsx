import { FC } from "react";

interface Props {
    customerId?: number;
    propertyId?: number;
}

const ViewAll: FC<Props> = ({ customerId, propertyId }) => {
    return null;
};

export default ViewAll;
