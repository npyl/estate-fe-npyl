import { IKanbanColumn } from "@/types/tasks";
import { FC } from "react";

interface ListProps {
    columns: IKanbanColumn[];
}

const List: FC<ListProps> = ({ columns }) => null;

export default List;
