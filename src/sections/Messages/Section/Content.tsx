import Stack from "@mui/material/Stack";
import { FC } from "react";

interface MessageContentProps {
    userId?: number;
}

const MessageContent: FC<MessageContentProps> = ({ userId }) => <Stack></Stack>;

export default MessageContent;
