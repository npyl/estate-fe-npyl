type StateType = {
    managerId: string;
    ownerId: string;
    zipCode: string;
    area: string;
    labels: number[];
    bedrooms: string;
    state: string;
    publicSites: number[];
    exclusive: boolean;
};

export type { StateType };
