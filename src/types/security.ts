export interface SecurityPOST {
    parentCategory?: string;
    category?: string;
    state?: string;
}

export interface Security {
    parentCategory: string;
    category: string;
    state: string;
}
